# clawtop

A minimal, read-only TUI (htop-style) for OpenClaw runtime status.

## Goals

- Single-screen overview: sessions/subagents, tasks, crons, token history, host load
- Default refresh: 2s (configurable)
- Credential-free: reads local state files only

## Status

WIP.
